# cloting-store
## Dependencies
- React w/ Router
- Tensorflow 
- Posenet model
- opencv-python

## Getting Started
1. Install all dependencies using `npm install` .
2. Start the web server using the `npm start` command. The app will be served at <http://localhost:3000/>.
3. Go to <http://localhost:3000/> in your browse
